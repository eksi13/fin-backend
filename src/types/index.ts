export enum TransactionType {
  Expense = 0,
  Income = 1,
  TransferPlus = 2,
  TransferMinus = 3,
}

export enum AccountType {
  Checking = 0,
  Savings = 1,
  Cash = 2,
  Investment = 3,
}

export enum Currency {
  EUR = 0,
}
